services:
  backend:
    build: ./backend
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017/taskdb

  mongo:
    image: mongo
    restart: always
    volumes:
      - mongo-data:/data/db

  test:
    build: ./tests
    command: npm test
    depends_on:
      - backend
      - mongo
    volumes:
      - ./tests:/app
      - test-node-modules:/app/node_modules

volumes:
  mongo-data:
  test-node-modules: